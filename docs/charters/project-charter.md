# Project Charter

在此填写整个仓库统一生效的项目级宪章。

建议包含：

- 项目目标与非目标
- 权威需求来源
- 不可违反的治理边界
- 多应用协作原则
- 文档、代码、脚本的统一约束
- 所有参数来自于 .env.local.L4 文件，默认不要修改该文件，如果必须修改，需要征求我的允许
- 项目开发，设计过程中，不得创建，或者使用我允许或者配置文件中允许的云资源以外的云资源。如果必须获得我的许可。
- 在开发和部署的时候，必须遵守架构设计文档中的设计。如果超出设计，则必须获得我的许可。